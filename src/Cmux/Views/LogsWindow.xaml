<Window x:Class="Cmux.Views.LogsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Logs_Title}" Height="660" Width="1120"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="34" CornerRadius="0" GlassFrameThickness="0"
                      ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>

    <Window.Resources>

        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" />
        </Style>

        <Style x:Key="DarkListHeader" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{DynamicResource SidebarItemHoverBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="ActionButton" TargetType="Button" BasedOn="{StaticResource CmuxButton}">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,6" />
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button" BasedOn="{StaticResource AccentButton}">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,6" />
        </Style>

        <Style x:Key="DarkListItem" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverOverlayBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SidebarItemSelectedBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}"
            CornerRadius="{StaticResource WindowCornerRadius}"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource SidebarBackgroundBrush}"
                    CornerRadius="10,10,0,0"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="{DynamicResource Logs_Title}" Foreground="{DynamicResource ForegroundDimBrush}"
                               FontSize="12" VerticalAlignment="Center" Margin="14,0,0,0" />
                    <Button Style="{StaticResource WindowCloseButton}" HorizontalAlignment="Right"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Click="Close_Click" Width="38" Height="34">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="9" />
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,8">
                    <DockPanel Margin="0,0,0,8">
                        <TextBlock Text="{DynamicResource Logs_Date}" Foreground="{DynamicResource ForegroundDimBrush}"
                                   VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="DateCombo" Width="160" SelectionChanged="DateCombo_SelectionChanged" />
                        <Button Content="{DynamicResource Logs_Refresh}" Margin="8,0,0,0" Padding="10,4" Style="{StaticResource CmuxButton}" Click="Refresh_Click" />
                        <Button Content="{DynamicResource Logs_OpenFolder}" Margin="8,0,0,0" Padding="10,4" Style="{StaticResource CmuxButton}" Click="OpenFolder_Click" />
                        <Button Content="{DynamicResource Logs_OpenCaptures}" Margin="8,0,0,0" Padding="10,4" Style="{StaticResource CmuxButton}" Click="OpenTranscriptsFolder_Click" />
                        <Button Content="{DynamicResource Logs_SessionVault}" Margin="8,0,0,0" Padding="10,4" Style="{StaticResource CmuxButton}" Click="OpenSessionVault_Click" />
                        <TextBlock x:Name="SummaryText" Margin="16,0,0,0" VerticalAlignment="Center"
                                   Foreground="{DynamicResource ForegroundDimBrush}" />
                    </DockPanel>

                    <WrapPanel>
                        <TextBlock Text="{DynamicResource Logs_Workspace}" Foreground="{DynamicResource ForegroundDimBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="WorkspaceFilterCombo" Width="220"                                  SelectionChanged="WorkspaceFilterCombo_SelectionChanged" Margin="0,0,12,0" />

                        <TextBlock Text="{DynamicResource Logs_Surface}" Foreground="{DynamicResource ForegroundDimBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="SurfaceFilterCombo" Width="220"                                  SelectionChanged="SurfaceFilterCombo_SelectionChanged" Margin="0,0,12,0" />

                        <TextBlock Text="{DynamicResource Logs_Pane}" Foreground="{DynamicResource ForegroundDimBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="PaneFilterCombo" Width="160"                                  SelectionChanged="PaneFilterCombo_SelectionChanged" Margin="0,0,12,0" />

                        <TextBlock Text="{DynamicResource Logs_Search}" Foreground="{DynamicResource ForegroundDimBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox x:Name="SearchBox" Width="220" Style="{StaticResource CmuxTextBox}" TextChanged="SearchBox_TextChanged" Margin="0,0,12,0" />

                        <Button Content="{DynamicResource Logs_ClearFilters}" Padding="10,4" Style="{StaticResource CmuxButton}" Click="ClearFilters_Click" />
                    </WrapPanel>
                </StackPanel>

                <ListView Grid.Row="1" x:Name="EntriesList"
                          Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource ForegroundBrush}"
                          BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                          ItemContainerStyle="{StaticResource DarkListItem}"
                          MouseDoubleClick="EntriesList_MouseDoubleClick"
                          PreviewKeyDown="EntriesList_PreviewKeyDown">
                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader" BasedOn="{StaticResource DarkListHeader}" />
                    </ListView.Resources>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="{DynamicResource Logs_ColTime}" Width="78" DisplayMemberBinding="{Binding StartedAtLocal}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColWorkspace}" Width="210" DisplayMemberBinding="{Binding WorkspaceDisplay}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColSurface}" Width="180" DisplayMemberBinding="{Binding SurfaceDisplay}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColPane}" Width="90" DisplayMemberBinding="{Binding PaneDisplay}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColCommand}" Width="300" DisplayMemberBinding="{Binding Command}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColWorkingDir}" Width="220" DisplayMemberBinding="{Binding WorkingDirectoryDisplay}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColExit}" Width="50" DisplayMemberBinding="{Binding ExitCodeText}" />
                            <GridViewColumn Header="{DynamicResource Logs_ColDuration}" Width="90" DisplayMemberBinding="{Binding DurationDisplay}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="{DynamicResource Logs_CopyCommand}" Style="{StaticResource ActionButton}" Click="CopyCommand_Click" />
                    <Button Content="{DynamicResource Logs_InsertInPane}" Style="{StaticResource ActionButton}" Click="InsertCommand_Click" />
                    <Button Content="{DynamicResource Logs_RunInPane}" Style="{StaticResource PrimaryActionButton}" Click="RunCommand_Click" />
                    <Button Content="{DynamicResource Logs_Close}" Style="{StaticResource CmuxButton}" Padding="12,6" Click="Close_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
