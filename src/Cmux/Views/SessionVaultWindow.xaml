<Window x:Class="Cmux.Views.SessionVaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Vault_Title}" Height="700" Width="1280"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="34" CornerRadius="0" GlassFrameThickness="0"
                      ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="VaultListItem" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="RowBg" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                 Columns="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=View.Columns}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RowBg" Property="Background" Value="{DynamicResource HoverOverlayBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RowBg" Property="Background" Value="{DynamicResource SidebarItemSelectedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="VaultHeader" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{DynamicResource SidebarItemHoverBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}"
            CornerRadius="{StaticResource WindowCornerRadius}"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource SidebarBackgroundBrush}"
                    CornerRadius="10,10,0,0"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="{DynamicResource Vault_Title}" Foreground="{DynamicResource ForegroundDimBrush}"
                               FontSize="12" VerticalAlignment="Center" Margin="14,0,0,0" />
                    <Button Style="{StaticResource WindowCloseButton}" HorizontalAlignment="Right"
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Click="Close_Click" Width="38" Height="34">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="9" />
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="{DynamicResource Vault_Search}" Foreground="{DynamicResource ForegroundDimBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBox x:Name="SearchBox" Width="320" Style="{StaticResource CmuxTextBox}"
                             TextChanged="SearchBox_TextChanged" />
                    <TextBlock x:Name="SummaryText" Margin="16,0,0,0" VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundDimBrush}" />
                    <Button Content="{DynamicResource Vault_Refresh}" Style="{StaticResource CmuxButton}" Margin="8,0,0,0" Padding="10,4" Click="Refresh_Click" />
                    <Button Content="{DynamicResource Vault_OpenFolder}" Style="{StaticResource CmuxButton}" Margin="8,0,0,0" Padding="10,4" Click="OpenFolder_Click" />
                </DockPanel>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="420" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                            Background="{DynamicResource SurfaceBrush}">
                        <ListView x:Name="EntriesList" ItemContainerStyle="{StaticResource VaultListItem}"
                                  Background="{DynamicResource SurfaceBrush}" Foreground="{DynamicResource ForegroundBrush}" BorderThickness="0"
                                  MouseDoubleClick="EntriesList_MouseDoubleClick"
                                  SelectionChanged="EntriesList_SelectionChanged">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader" BasedOn="{StaticResource VaultHeader}" />
                            </ListView.Resources>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="{DynamicResource Vault_ColTime}" Width="120" DisplayMemberBinding="{Binding CapturedAtLocal}" />
                                    <GridViewColumn Header="{DynamicResource Vault_ColWorkspace}" Width="150" DisplayMemberBinding="{Binding WorkspaceDisplay}" />
                                    <GridViewColumn Header="{DynamicResource Vault_ColSurface}" Width="120" DisplayMemberBinding="{Binding SurfaceDisplay}" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch"
                                  Background="{DynamicResource DividerBrush}" ShowsPreview="False" />

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" Padding="10,8" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock x:Name="MetaTitleText" Text="{DynamicResource Vault_SelectCapture}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" />
                                <TextBlock x:Name="MetaInfoText" Foreground="{DynamicResource ForegroundDimBrush}" FontSize="12" Margin="0,4,0,0" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                Background="{DynamicResource BackgroundBrush}" CornerRadius="4">
                            <TextBox x:Name="TranscriptText"
                                     IsReadOnly="True"
                                     TextWrapping="NoWrap"
                                     AcceptsReturn="True"
                                     HorizontalScrollBarVisibility="Auto"
                                     VerticalScrollBarVisibility="Auto"
                                     Background="{DynamicResource BackgroundBrush}"
                                     Foreground="{DynamicResource ForegroundBrush}"
                                     BorderThickness="0"
                                     Padding="12"
                                     FontFamily="Cascadia Code"
                                     FontSize="13" />
                        </Border>
                    </Grid>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="{DynamicResource Vault_CopyAll}" Style="{StaticResource CmuxButton}" Margin="0,0,8,0" Padding="12,6" Click="CopyAll_Click" />
                    <Button Content="{DynamicResource Vault_OpenFile}" Style="{StaticResource CmuxButton}" Margin="0,0,8,0" Padding="12,6" Click="OpenFile_Click" />
                    <Button Content="{DynamicResource Vault_Close}" Style="{StaticResource AccentButton}" Padding="12,6" Click="Close_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
