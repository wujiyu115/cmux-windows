<Window x:Class="Cmux.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Settings_Title}" Height="600" Width="700"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Nav button style -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundDimBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Margin="4,1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#20FFFFFF" />
                            </Trigger>
                            <Trigger Property="Tag" Value="active">
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                                <Setter TargetName="Bd" Property="Background" Value="#30FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Settings text input style -->
        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}" />
        </Style>

        <Style x:Key="SettingsPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}" />
        </Style>

<!-- Settings checkbox style -->
        <Style x:Key="SettingsCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <!-- Section label -->
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundDimBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="SettingsListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Padding" Value="2" />
        </Style>

        <Style x:Key="SettingsListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="6,4" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SidebarItemSelectedBrush}" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SidebarItemHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="10" BorderThickness="1"
            BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.6" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="{DynamicResource Settings_Title}" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource ForegroundBrush}"
                               VerticalAlignment="Center" Margin="18,0,0,0" />
                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Margin="0,0,10,0" Click="CloseButton_Click"
                            Style="{StaticResource IconButton}" Content="&#xE8BB;"
                            FontFamily="Segoe MDL2 Assets" FontSize="12" />
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Nav Sidebar -->
                <StackPanel Grid.Column="0" Margin="0,8,0,0">
                    <Button x:Name="NavAppearance" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{DynamicResource Settings_Appearance}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavTerminal" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{DynamicResource Settings_Terminal}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavBehavior" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{DynamicResource Settings_Behavior}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavKeyboard" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE765;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{DynamicResource Settings_Keyboard}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavAgent" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE943;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{DynamicResource Settings_Agent}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavAbout" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{DynamicResource Settings_About}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Divider -->
                <Border Grid.Column="1" Background="{DynamicResource BorderBrush}" />

                <!-- Content Area -->
                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Padding="24,16">
                    <Grid>

                    <!-- ═══ Appearance Section ═══ -->
                    <StackPanel x:Name="AppearanceSection">
                        <TextBlock Text="{DynamicResource Settings_Appearance}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- Language -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Language}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="LanguageCombo" Grid.Column="1"
                                      Width="200" HorizontalAlignment="Left"
                                      SelectionChanged="LanguageCombo_SelectionChanged" />
                        </Grid>

                        <!-- Font Family -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_FontFamily}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="FontFamilyCombo" Grid.Column="1"
                                      Width="320" HorizontalAlignment="Left"
                                      IsTextSearchEnabled="True" />
                        </Grid>

                        <!-- Font Size -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_FontSize}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Slider x:Name="FontSizeSlider" Minimum="9" Maximum="28"
                                        Width="220" VerticalAlignment="Center"
                                        TickFrequency="1" IsSnapToTickEnabled="True"
                                        ValueChanged="FontSizeSlider_ValueChanged" />
                                <TextBlock x:Name="FontSizeValueText" Foreground="{DynamicResource ForegroundDimBrush}"
                                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" />
                            </StackPanel>
                        </Grid>

                        <!-- Theme -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Theme}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ComboBox x:Name="ThemeCombo"
                                          Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}"
                                          Width="200" SelectionChanged="ThemeCombo_SelectionChanged" />
                                <Border x:Name="ThemePreview" Width="60" Height="30" Margin="12,0,0,0"
                                        CornerRadius="4" BorderThickness="1"
                                        BorderBrush="{DynamicResource BorderBrush}">
                                    <TextBlock x:Name="ThemePreviewText" Text="Aa" FontSize="13"
                                               FontWeight="SemiBold" HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- Opacity -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Opacity}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Slider x:Name="OpacitySlider" Minimum="0.5" Maximum="1.0"
                                        Width="200" VerticalAlignment="Center"
                                        TickFrequency="0.01" IsSnapToTickEnabled="True"
                                        ValueChanged="OpacitySlider_ValueChanged" />
                                <TextBlock x:Name="OpacityValueText" Foreground="{DynamicResource ForegroundDimBrush}"
                                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" />
                            </StackPanel>
                        </Grid>

                        <!-- Cursor Style -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CursorStyle}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="CursorStyleCombo" Grid.Column="1"
                                      Width="140" HorizontalAlignment="Left" />
                        </Grid>

                        <!-- Cursor Blink -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CursorBlink}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CursorBlinkCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}" />
                        </Grid>

                        <!-- System Theme (detected) -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_SystemTheme}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="SystemThemeText" Text="Dark" FontSize="13"
                                           Foreground="{DynamicResource ForegroundBrush}" />
                                <TextBlock Text="{DynamicResource Settings_Detected}" FontSize="12" Foreground="{DynamicResource ForegroundDimBrush}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <!-- ═══ Terminal Section ═══ -->
                    <StackPanel x:Name="TerminalSection" Visibility="Collapsed">
                        <TextBlock Text="{DynamicResource Settings_Terminal}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- Color Preset -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_ColorPreset}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="TerminalThemePresetCombo" Grid.Column="1"
                                      Width="240" HorizontalAlignment="Left"
                                      SelectionChanged="TerminalThemePresetCombo_SelectionChanged" />
                        </Grid>

                        <!-- Use custom colors -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CustomColors}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="UseCustomTerminalColorsCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_OverrideColors}"
                                      Checked="UseCustomTerminalColorsCheck_Changed"
                                      Unchecked="UseCustomTerminalColorsCheck_Changed" />
                        </Grid>

                        <!-- Terminal color swatches -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Background}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalBackgroundColorPanel">
                                <Border x:Name="TerminalBackgroundPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalBackgroundHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="{DynamicResource Settings_Pick}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                        Click="PickTerminalBackgroundColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Foreground}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalForegroundColorPanel">
                                <Border x:Name="TerminalForegroundPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalForegroundHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="{DynamicResource Settings_Pick}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                        Click="PickTerminalForegroundColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Cursor}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalCursorColorPanel">
                                <Border x:Name="TerminalCursorPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalCursorHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="{DynamicResource Settings_Pick}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                        Click="PickTerminalCursorColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_Selection}" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalSelectionColorPanel">
                                <Border x:Name="TerminalSelectionPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalSelectionHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="{DynamicResource Settings_Pick}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                        Click="PickTerminalSelectionColor_Click" />
                                <Button Content="{DynamicResource Settings_Reset}" Style="{StaticResource CmuxButton}" Padding="10,5" Margin="8,0,0,0"
                                        Click="ResetTerminalColors_Click" />
                            </StackPanel>
                        </Grid>

                        <!-- Default Shell -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_DefaultShell}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="ShellCombo" Grid.Column="1" />
                        </Grid>

                        <!-- Shell Arguments -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_ShellArgs}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="ShellArgsBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" />
                        </Grid>

                        <!-- Scrollback Lines -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_ScrollbackLines}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="ScrollbackBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="100"
                                     HorizontalAlignment="Left" />
                        </Grid>

                        <!-- Visual Bell -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_VisualBell}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="VisualBellCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}" />
                        </Grid>

                        <!-- Bracketed Paste -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_BracketedPaste}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="BracketedPasteCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}" />
                        </Grid>
                    </StackPanel>

                    <!-- ═══ Behavior Section ═══ -->
                    <StackPanel x:Name="BehaviorSection" Visibility="Collapsed">
                        <TextBlock Text="{DynamicResource Settings_Behavior}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- Restore Session -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_RestoreSession}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="RestoreSessionCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_RestoreSessionDesc}" />
                        </Grid>

                        <!-- Confirm on Close -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_ConfirmClose}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="ConfirmCloseCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_ConfirmCloseDesc}" />
                        </Grid>

                        <!-- Auto Copy -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_AutoCopy}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="AutoCopyCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_AutoCopyDesc}" />
                        </Grid>

                        <!-- Ctrl+Click URLs -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CtrlClickUrls}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CtrlClickUrlCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_CtrlClickUrlsDesc}" />
                        </Grid>

                        <!-- Agent Chat Default Open -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_AgentChat}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="AgentChatDefaultOpenCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_AgentChatDesc}" />
                        </Grid>

                        <!-- Auto Save Interval -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_AutoSave}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="AutoSaveBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="80"
                                     HorizontalAlignment="Left" />
                        </Grid>

                        <!-- Log Retention -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_LogRetention}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="LogRetentionDaysBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="80"
                                     HorizontalAlignment="Left" ToolTip="{DynamicResource Settings_LogRetentionTooltip}" />
                        </Grid>

                        <!-- Capture transcript on close -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CaptureOnClose}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CaptureOnCloseCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_CaptureOnCloseDesc}" />
                        </Grid>

                        <!-- Capture transcript on clear -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CaptureOnClear}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CaptureOnClearCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_CaptureOnClearDesc}" />
                        </Grid>

                        <!-- Transcript retention -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CaptureRetention}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="TranscriptRetentionDaysBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="80"
                                     HorizontalAlignment="Left" ToolTip="{DynamicResource Settings_CaptureRetentionTooltip}" />
                        </Grid>
                    </StackPanel>

                    <!-- ═══ Keyboard Section ═══ -->
                    <StackPanel x:Name="KeyboardSection" Visibility="Collapsed">
                        <TextBlock Text="{DynamicResource Settings_KeyboardShortcuts}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="16,12" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                </Grid.RowDefinitions>

                                <!-- Workspaces -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Ctrl+N" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{DynamicResource Shortcut_NewWorkspace}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl+1-8" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{DynamicResource Shortcut_JumpWorkspace}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Ctrl+Shift+W" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{DynamicResource Shortcut_CloseWorkspace}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- Surfaces -->
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Ctrl+T" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{DynamicResource Shortcut_NewSurface}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Ctrl+W" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{DynamicResource Shortcut_CloseSurface}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Ctrl+Tab" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="5" Grid.Column="1" Text="{DynamicResource Shortcut_NextSurface}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Ctrl+Shift+Tab" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="6" Grid.Column="1" Text="{DynamicResource Shortcut_PrevSurface}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- Panes -->
                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Ctrl+D" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="7" Grid.Column="1" Text="{DynamicResource Shortcut_SplitRight}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="8" Grid.Column="0" Text="Ctrl+Shift+D" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="8" Grid.Column="1" Text="{DynamicResource Shortcut_SplitDown}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="9" Grid.Column="0" Text="Ctrl+Alt+Arrow" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="9" Grid.Column="1" Text="{DynamicResource Shortcut_FocusPane}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="10" Grid.Column="0" Text="Ctrl+Shift+Z" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="10" Grid.Column="1" Text="{DynamicResource Shortcut_ZoomToggle}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- Terminal -->
                                <TextBlock Grid.Row="11" Grid.Column="0" Text="Ctrl+Backspace" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="11" Grid.Column="1" Text="{DynamicResource Shortcut_DeleteWord}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- View -->
                                <TextBlock Grid.Row="12" Grid.Column="0" Text="Ctrl+B" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="12" Grid.Column="1" Text="{DynamicResource Shortcut_ToggleSidebar}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="13" Grid.Column="0" Text="Ctrl+I" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="13" Grid.Column="1" Text="{DynamicResource Shortcut_NotificationPanel}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="14" Grid.Column="0" Text="Ctrl+Shift+U" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="14" Grid.Column="1" Text="{DynamicResource Shortcut_JumpUnread}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="15" Grid.Column="0" Text="Ctrl+Shift+F" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="15" Grid.Column="1" Text="{DynamicResource Shortcut_SearchTerminal}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="16" Grid.Column="0" Text="Ctrl+Shift+P" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="16" Grid.Column="1" Text="{DynamicResource Shortcut_CommandPalette}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="17" Grid.Column="0" Text="Ctrl+Shift+A" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="17" Grid.Column="1" Text="{DynamicResource Shortcut_ToggleAgentChat}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="18" Grid.Column="0" Text="Ctrl+Shift+L" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="18" Grid.Column="1" Text="{DynamicResource Shortcut_OpenLogs}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="19" Grid.Column="0" Text="Ctrl+Alt+H" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="19" Grid.Column="1" Text="{DynamicResource Shortcut_OpenHistory}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="20" Grid.Column="0" Text="Ctrl+Shift+V" FontWeight="SemiBold"
                                           Foreground="{DynamicResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="20" Grid.Column="1" Text="{DynamicResource Shortcut_OpenVault}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- ═══ Agent Section ═══ -->
                    <StackPanel x:Name="AgentSection" Visibility="Collapsed">
                        <TextBlock Text="{DynamicResource Settings_Agent}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_EnableAgent}" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="AgentEnabledCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}"
                                      Content="{DynamicResource Settings_EnableAgentDesc}" />
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_AgentName}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="AgentNameBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="220" HorizontalAlignment="Left" />
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_PrimaryHandler}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="AgentHandlerBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="220" HorizontalAlignment="Left" />
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_ExtraHandlers}" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="AgentAdditionalHandlersBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}"
                                     ToolTip="{DynamicResource Settings_ExtraHandlersTooltip}" />
                        </Grid>

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_ActiveProvider}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="AgentProviderCombo" Grid.Column="1" Width="220" HorizontalAlignment="Left">
                                <ComboBoxItem Content="{DynamicResource Provider_OpenAI}" />
                                <ComboBoxItem Content="{DynamicResource Provider_Anthropic}" />
                            </ComboBox>
                        </Grid>

                        <TextBlock Text="{DynamicResource Settings_SystemPrompt}" Style="{StaticResource SectionLabel}" Margin="0,0,0,6" />
                        <TextBox x:Name="AgentSystemPromptBox" Style="{StaticResource SettingsTextBox}" Height="96"
                                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                 TextWrapping="Wrap" Margin="0,0,0,12" />

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_OpenAI}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_BaseURL}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="OpenAiBaseUrlBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_Model}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="OpenAiModelBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_APIKey}" Style="{StaticResource SectionLabel}" />
                                    <PasswordBox x:Name="OpenAiApiKeyBox" Grid.Column="1" Style="{StaticResource SettingsPasswordBox}" />
                                    <Button Grid.Column="2" Content="{DynamicResource Settings_Clear}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                            Margin="8,0,0,0" Click="ClearOpenAiKey_Click" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_Anthropic}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_BaseURL}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AnthropicBaseUrlBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_Model}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AnthropicModelBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_APIKey}" Style="{StaticResource SectionLabel}" />
                                    <PasswordBox x:Name="AnthropicApiKeyBox" Grid.Column="1" Style="{StaticResource SettingsPasswordBox}" />
                                    <Button Grid.Column="2" Content="{DynamicResource Settings_Clear}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                            Margin="8,0,0,0" Click="ClearAnthropicKey_Click" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_Tools}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_BashTool}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentBashToolCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_BashTimeout}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentBashTimeoutBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="100" HorizontalAlignment="Left" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_WebSearch}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentWebSearchCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_ExaBaseURL}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="ExaBaseUrlBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_ExaAPIKey}" Style="{StaticResource SectionLabel}" />
                                    <PasswordBox x:Name="ExaApiKeyBox" Grid.Column="1" Style="{StaticResource SettingsPasswordBox}" />
                                <Button Grid.Column="2" Content="{DynamicResource Settings_Clear}" Style="{StaticResource CmuxButton}" Padding="10,5"
                                            Margin="8,0,0,0" Click="ClearExaKey_Click" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_PaneSubmit}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_DefaultSubmitKey}" Style="{StaticResource SectionLabel}" />
                                    <ComboBox x:Name="AgentDefaultSubmitKeyCombo" Grid.Column="1" Width="220" HorizontalAlignment="Left">
                                        <ComboBoxItem Content="{DynamicResource Submit_Auto}" />
                                        <ComboBoxItem Content="{DynamicResource Submit_Enter}" />
                                        <ComboBoxItem Content="{DynamicResource Submit_Linefeed}" />
                                        <ComboBoxItem Content="{DynamicResource Submit_Crlf}" />
                                    </ComboBox>
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_EnableAutoFallback}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentEnableSubmitFallbackCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_FallbackWait}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentSubmitFallbackWaitMsBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="120" HorizontalAlignment="Left" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_FallbackOrder}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentSubmitFallbackOrderBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}"
                                             ToolTip="{DynamicResource Settings_FallbackOrderTooltip}" />
                                </Grid>

                                <Grid Margin="0,10,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_EnableSubmitProfiles}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentEnableSubmitProfilesCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <TextBlock Text="{DynamicResource Settings_SubmitProfilesDesc}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,2,0,6" />
                                <TextBox x:Name="AgentSubmitProfilesJsonBox" Style="{StaticResource SettingsTextBox}" Height="120"
                                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                         TextWrapping="NoWrap" FontFamily="Cascadia Code" FontSize="12"
                                         ToolTip="{DynamicResource Settings_SubmitProfilesTooltip}" />
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_AgentFiles}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_AutoDiscover}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentAutoDiscoverFilesCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_InstructionsPath}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentInstructionsPathBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}"
                                             ToolTip="{DynamicResource Settings_InstructionsPathTooltip}" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_SkillsRootPath}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentSkillsRootPathBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}"
                                             ToolTip="{DynamicResource Settings_SkillsRootPathTooltip}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_ChatPanel}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_ChatFontFamily}" Style="{StaticResource SectionLabel}" />
                                    <ComboBox x:Name="AgentChatFontFamilyCombo" Grid.Column="1"                                              IsEditable="True" IsTextSearchEnabled="True" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_ChatFontSize}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentChatFontSizeBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="100" HorizontalAlignment="Left" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="14,12" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_ConversationMemory}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,10" />

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_EnableMemory}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentMemoryCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_EnableStreaming}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentStreamingCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_AutoCompactContext}" Style="{StaticResource SectionLabel}" />
                                    <CheckBox x:Name="AgentAutoCompactCheck" Grid.Column="1" Style="{StaticResource SettingsCheck}" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_MaxContextMessages}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentContextMaxMessagesBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="100" HorizontalAlignment="Left" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_ContextBudgetTokens}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentContextBudgetTokensBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="120" HorizontalAlignment="Left" />
                                </Grid>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_CompactThreshold}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentCompactThresholdBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="80" HorizontalAlignment="Left" />
                                </Grid>

                                <Grid Margin="0,0,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource Settings_KeepRecentOnCompact}" Style="{StaticResource SectionLabel}" />
                                    <TextBox x:Name="AgentKeepRecentOnCompactionBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" Width="100" HorizontalAlignment="Left" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,2,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_CustomToolsMode}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="CustomToolsModeCombo" Grid.Column="1" Width="220"
                                      HorizontalAlignment="Left" SelectionChanged="CustomToolsModeCombo_SelectionChanged">
                                <ComboBoxItem Content="{DynamicResource Settings_Creator}" />
                                <ComboBoxItem Content="{DynamicResource Settings_JSON}" />
                            </ComboBox>
                        </Grid>

                        <StackPanel x:Name="CustomToolsCreatorPanel" Margin="0,0,0,10">
                            <ListBox x:Name="CustomToolsListBox" Height="100" DisplayMemberPath="Name"
                                     Style="{StaticResource SettingsListBox}" ItemContainerStyle="{StaticResource SettingsListBoxItem}"
                                     SelectionChanged="CustomToolsListBox_SelectionChanged" Margin="0,0,0,8" />
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_ToolName}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="CustomToolNameBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_ToolDescription}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="CustomToolDescriptionBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_ToolCommandTemplate}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="CustomToolCommandTemplateBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <CheckBox x:Name="CustomToolEnabledCheck" Content="{DynamicResource Settings_ToolEnabled}" Style="{StaticResource SettingsCheck}" Margin="0,0,0,6" />
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{DynamicResource Settings_AddUpdate}" Style="{StaticResource CmuxButton}" Padding="12,6" Click="AddOrUpdateCustomTool_Click" />
                                <Button Content="{DynamicResource Settings_RemoveSelected}" Style="{StaticResource CmuxButton}" Padding="12,6" Margin="8,0,0,0"
                                        Click="RemoveSelectedCustomTool_Click" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel x:Name="CustomToolsJsonPanel" Visibility="Collapsed" Margin="0,0,0,10">
                            <TextBlock Text="Custom Tools JSON (array of { enabled, name, description, commandTemplate })"
                                       Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,2,0,6" />
                            <TextBox x:Name="CustomToolsJsonBox" Style="{StaticResource SettingsTextBox}" Height="120"
                                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                     TextWrapping="NoWrap" FontFamily="Cascadia Code" FontSize="12" />
                        </StackPanel>

                        <Grid Margin="0,2,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{DynamicResource Settings_McpServersMode}" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="McpServersModeCombo" Grid.Column="1" Width="220"
                                      HorizontalAlignment="Left" SelectionChanged="McpServersModeCombo_SelectionChanged">
                                <ComboBoxItem Content="{DynamicResource Settings_Creator}" />
                                <ComboBoxItem Content="{DynamicResource Settings_JSON}" />
                            </ComboBox>
                        </Grid>

                        <StackPanel x:Name="McpServersCreatorPanel">
                            <ListBox x:Name="McpServersListBox" Height="100" DisplayMemberPath="Name"
                                     Style="{StaticResource SettingsListBox}" ItemContainerStyle="{StaticResource SettingsListBoxItem}"
                                     SelectionChanged="McpServersListBox_SelectionChanged" Margin="0,0,0,8" />
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_ToolName}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="McpServerNameBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_McpCommand}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="McpServerCommandBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_McpArguments}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="McpServerArgumentsBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{DynamicResource Settings_McpWorkingDir}" Style="{StaticResource SectionLabel}" />
                                <TextBox x:Name="McpServerWorkingDirectoryBox" Grid.Column="1" Style="{StaticResource SettingsTextBox}" />
                            </Grid>
                            <CheckBox x:Name="McpServerEnabledCheck" Content="{DynamicResource Settings_ToolEnabled}" Style="{StaticResource SettingsCheck}" Margin="0,0,0,6" />
                            <StackPanel Orientation="Horizontal">
                                <Button Content="{DynamicResource Settings_AddUpdate}" Style="{StaticResource CmuxButton}" Padding="12,6" Click="AddOrUpdateMcpServer_Click" />
                                <Button Content="{DynamicResource Settings_RemoveSelected}" Style="{StaticResource CmuxButton}" Padding="12,6" Margin="8,0,0,0"
                                        Click="RemoveSelectedMcpServer_Click" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel x:Name="McpServersJsonPanel" Visibility="Collapsed">
                            <TextBlock Text="MCP Servers JSON (array of { enabled, name, command, arguments, workingDirectory })"
                                       Foreground="{DynamicResource ForegroundDimBrush}" Margin="0,2,0,6" />
                            <TextBox x:Name="McpServersJsonBox" Style="{StaticResource SettingsTextBox}" Height="120"
                                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                     TextWrapping="NoWrap" FontFamily="Cascadia Code" FontSize="12" />
                        </StackPanel>
                    </StackPanel>

                    <!-- ═══ About Section ═══ -->
                    <StackPanel x:Name="AboutSection" Visibility="Collapsed">
                        <TextBlock Text="{DynamicResource Settings_About}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Border Background="{DynamicResource InputBackgroundBrush}" CornerRadius="6" Padding="20,16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource Settings_AboutTitle}" FontSize="20" FontWeight="Bold"
                                           Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,4" />
                                <TextBlock Text="{DynamicResource Settings_AboutVersion}" FontSize="13"
                                           Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,12" />
                                <TextBlock Text="{DynamicResource Settings_AboutDesc}"
                                           Foreground="{DynamicResource ForegroundDimBrush}" FontSize="13"
                                           TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12" />
                                <Border Height="1" Background="{DynamicResource BorderBrush}" Margin="0,4,0,12" />
                                <TextBlock Foreground="{DynamicResource ForegroundDimBrush}" FontSize="12">
                                    <Run Text="{DynamicResource Settings_AboutRuntime}" /><LineBreak />
                                    <Run Text="{DynamicResource Settings_AboutFramework}" /><LineBreak />
                                    <Run Text="{DynamicResource Settings_AboutConfig}" />
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>

            <!-- Bottom Bar -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}"
                    Padding="18,12">
                <Grid>
                    <Button Content="{DynamicResource Settings_ResetDefaults}" Style="{StaticResource CmuxButton}"
                            Click="Reset_Click" HorizontalAlignment="Left" Padding="14,7" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="{DynamicResource Settings_Cancel}" Style="{StaticResource CmuxButton}"
                                Click="Cancel_Click" Padding="16,7" Margin="0,0,8,0" />
                        <Button Content="{DynamicResource Settings_Save}" Style="{StaticResource AccentButton}"
                                Click="Save_Click" Padding="20,7" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
